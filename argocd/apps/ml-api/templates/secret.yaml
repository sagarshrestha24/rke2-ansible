{{- if .Values.secret.enabled -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secret.name }}
type: Opaque
data:
  aws-access-key-id: {{ .Values.secret.awsAccessKeyId  }}
  aws-secret-access-key: {{ .Values.secret.awsSecretAccessKey }}
{{- end }}
