apiVersion: v1
kind: Service
metadata:
  name: {{ include "kg-api.fullname" . }}
  labels:
    {{- include "kg-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetport }}
      protocol: TCP
      name: http
  selector:
    app: kg-api
    {{- include "kg-api.selectorLabels" . | nindent 4 }}
